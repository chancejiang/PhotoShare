#!/usr/bin/env node

var CloudHook = require('../lib/cloud').CloudHook
    fs = require("fs");

// read my config dammit
var config = JSON.parse(fs.readFileSync(__dirname + '/../config.json'));
config.name = "cloud-control";
config.debug = true;

var myhook = new CloudHook(config);

myhook.start();
