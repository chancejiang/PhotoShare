#!/usr/bin/env node

var Registration = require('../lib/registration').Registration
    fs = require("fs");

// read my config dammit
var config = JSON.parse(fs.readFileSync(__dirname + '/../config.json'));
config.name = "registration";
config.debug = true;

var reg = new Registration(config);
reg.start();

// config.name = "channels";
// var chans = new ChannelsHook(config);
// chans.start();
